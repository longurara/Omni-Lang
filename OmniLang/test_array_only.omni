def main():
    print("=== Array Test Only ===")
    
    # 1. Test Array Literals
    arr = [1, 2, 3, 4, 5]
    print("Array[0]:", arr[0])
    print("Array[2]:", arr[2])
    print("Array length:", len(arr))
    
    # Empty array
    empty = []
    print("Empty array created")
    
    print("=== Done ===")
